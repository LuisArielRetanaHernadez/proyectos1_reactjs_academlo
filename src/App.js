//import logo from './logo.svg';
import './App.css';
import ContainerNotes from './component/QuoteBox'
function App() {
  // const index = Math.floor(Math.random() * quote.citas.length ) + 1
  // console.log(index, 'index', quote.citas[index])
  return (
    <div className="App">
      <ContainerNotes></ContainerNotes>
    </div>
  );
}

export default App;
